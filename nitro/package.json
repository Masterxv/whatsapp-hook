{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "start": "export $(cat .env | egrep -v '#|^$' | xargs) && node .output/server/index.mjs",
    "prepare": "nitro prepare",
    "preview": "nodemon .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "openai": "^4.28.4"
  }
}
