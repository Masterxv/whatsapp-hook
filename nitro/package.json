{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "no": "echo 1",
    "preview": "export $(cat .env | egrep -v '#|^$' | xargs) && node .output/server/index.mjs",
    "prepare": "nitro prepare"
  },
  "devDependencies": {
    "nitropack": "latest"
  },
  "dependencies": {
    "openai": "^4.28.4",
    "pocketbase": "^0.21.1"
  }
}
