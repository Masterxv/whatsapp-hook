{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "preview": "export $(cat .env | egrep -v '#|^$' | xargs) && node .output/server/index.mjs",
    "prepare": "nitro prepare"
  },
  "devDependencies": {
    "nitropack": "latest",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "openai": "^4.28.4"
  }
}
